import Link from "next/link";
import { ArrowRight, Quote } from "lucide-react";
import { events } from "@/data/rewind";
export default function QuotesPage(){const documented=events.filter(e=>e.eventTypes.some(t=>/Speech|Statement|Interview|Press/.test(t))).slice(0,24);return <div className="page-shell"><header className="page-hero"><span className="eyebrow">QUOTE REGISTER</span><h1>Words need coordinates.</h1><p>This edition exposes speech-bearing events while verbatim quotations and timestamps are transcribed and reviewed. It does not manufacture quotation text from summaries.</p></header><div className="quote-notice"><Quote/><div><b>Quotation integrity gate</b><p>These records contain attributable audiovisual or transcript evidence. Exact quotation entities will appear only after speaker, wording, language and timestamp review.</p></div></div><div className="quote-event-list">{documented.map(e=><Link href={`/event/${e.slug}`} key={e.id}><time>{e.startDate}</time><div><b>{e.eventName}</b><small>{e.city} · {e.medium.join(", ")}</small></div><ArrowRight/></Link>)}</div></div>}
