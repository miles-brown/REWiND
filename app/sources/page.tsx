import Link from "next/link";
import { ArrowRight, ExternalLink } from "lucide-react";
import { events, sources } from "@/data/rewind";
export default function SourcesPage(){return <div className="page-shell"><header className="page-hero"><span className="eyebrow">SOURCE REGISTER</span><h1>Follow every claim down.</h1><p>{sources.length} archival and documentary records currently support this edition. Sources remain separate from the events they evidence.</p></header><div className="source-table" role="table" aria-label="Source register"><div className="source-row source-head" role="row"><span>Source</span><span>Type</span><span>Class</span><span>Events</span><span/></div>{sources.map(s=><div className="source-row" role="row" key={s.id}><span><b>{s.title}</b><small>{s.publisher}</small></span><span>{s.sourceType.replaceAll("-"," ")}</span><span className={`source-class ${s.classification}`}>{s.classification}</span><span>{events.filter(e=>e.sourceIds.includes(s.id)).length}</span><span><Link href={`/source/${s.id}`} aria-label={`View ${s.title}`}><ArrowRight/></Link><a href={s.url} target="_blank" rel="noreferrer" aria-label={`Open external source ${s.title}`}><ExternalLink/></a></span></div>)}</div></div>}
